## sdk/rust/v1.0.0-beta.11.rust.4 - 2026-08-30

This SDK uses 🚙 Engine + 🚗 CLI version `v1.0.0-beta.11.rust.4`, built from this repository at the release commit; the complete engine ships as a release artifact alongside the crate files. The release line is now based on the real upstream `v1.0.0-beta.11` tag — the shared `beta.11` component of the version finally names the upstream release it merges.

📦 Released as checked repository artifacts (`dagger-sdk` and `dagger-sdk-macros` crate files plus the complete engines for both Linux architectures and the native macOS CLI); see sdk/rust/docs/namespace-build.md.

### Added
- The claimed schema advances to the pinned `v1.0.0-beta.11` revision and grows four types the previous capture predated — `Agent`, `AgentGroup`, `GitCommit`, `LLMSkill` — together with their fields, the definition-only `@agent` directive (registered target-inactive), `Workspace.withDirectory`, and `GitRepository.latest`; the generated client now exposes all of them by @iw in https://github.com/iw/dagger/pull/92
- Packaged Rust SDK content records its build platform in its OCI index, and engine composition refuses to reuse content built for another platform, so a cross-architecture composition fails loudly instead of shipping a foreign-architecture `dagger-rust-engine` helper by @iw in https://github.com/iw/dagger/pull/92 \
  `Build` threads its platform through content construction, and the ordinary release procedure now produces the `linux/arm64` engine through the same platform-checked path as `linux/amd64` (#90).
- Release verification gains a module-initialization gate against the completed engine: `Verify` installs the builtin Rust SDK and initializes a module, which resolves the packaged SDK dependency for real and executes the runtime helper on the engine's own platform by @iw in https://github.com/iw/dagger/pull/92 \
  A candidate whose packaged dependency cannot resolve (#91) or whose helper is the wrong architecture fails verification instead of shipping. The release invocation passes `--engine-repository`/`--sdk-dependency-revision` so packaged content selects the credential-free Git dependency at the pushed release commit.

### 🔥 Breaking Changes
- `GitRepository.latestVersion` is renamed to `latest` upstream and now returns the ref itself; generated callers of `latest_version` must move to `latest` — the one breaking change in the claimed surface across the upstream advance by @iw in https://github.com/iw/dagger/pull/92

### Fixed
- The checked schema is captured at the revision the target pins by @iw in https://github.com/iw/dagger/pull/92 \
  The previous capture predated the pinned `501b57e0` revision (visible as `GitRepository.commit` still returning `GitRef` in the checked document), so the claim and its bytes disagreed; the refresh procedure in sdk/rust/docs/maintaining.md now records the reproducible in-process capture so the pin and the document cannot drift silently again.
- Fresh Rust module and client initialization now preserves the complete packaged SDK context for generator policy and descriptor reads and scaffolds a compiler-valid root object, so both initialization paths complete against the shipped engine by @iw in https://github.com/iw/dagger/pull/94

### What to do next
- Read the [documentation](https://docs.dagger.io/sdk)
- Join our [Discord server](https://discord.gg/dagger-io)
- Follow us on [Twitter](https://twitter.com/dagger_io)
